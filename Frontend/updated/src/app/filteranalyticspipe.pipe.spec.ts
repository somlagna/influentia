import { FilteranalyticspipePipe } from './filteranalyticspipe.pipe';

describe('FilteranalyticspipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilteranalyticspipePipe();
    expect(pipe).toBeTruthy();
  });
});
