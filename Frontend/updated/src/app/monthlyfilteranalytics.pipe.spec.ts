import { MonthlyfilteranalyticsPipe } from './monthlyfilteranalytics.pipe';

describe('MonthlyfilteranalyticsPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthlyfilteranalyticsPipe();
    expect(pipe).toBeTruthy();
  });
});
